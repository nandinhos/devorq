# HANDOFF DEVORQ — 2026-04-07
## Destinatário: próximo LLM (Claude / Gemini / OpenCode)
## Gerado por: Claude Sonnet 4.6
## Projeto: DEVORQ — Meta-framework de orquestração de desenvolvimento
## Status: pendente

---

### CONTEXTO
- Stack: Bash puro (4.0+) — sem framework downstream
- Branch: main
- Último commit: 40eedd6 fix: corrigir 15 débitos técnicos identificados em code review profundo
- Status: 15 débitos técnicos corrigidos e commitados (92/93 testes passando)

---

### TAREFA
Incorporar a lição `2026-04-07-bash-dual-use-source-guard` em duas skills:

**1. `.devorq/skills/quality-gate/SKILL.md`**
Adicionar regra formal obrigatória:
> Scripts Bash dual-use (library + executable) DEVEM conter o guard
> `[[ "${BASH_SOURCE[0]}" != "$0" ]] && return 0`
> antes de qualquer bloco `case` ou lógica de execução direta.
Essa regra bloqueia o commit se violada.

**2. `.devorq/skills/pre-flight/SKILL.md`**
Adicionar verificação automática leve (não bloqueante — emite aviso):
> Varrer `lib/**/*.sh` em busca de arquivos que contenham `case` ou
> chamadas diretas no escopo global sem o guard de source.
> Orientar o dev a corrigir antes de chegar no quality-gate.

---

### CONSTRAINTS OBRIGATÓRIOS
- Runtime: `bash bin/devorq` — sem Docker, sem sail, sem PHP
- Dependências: `bash 4.0+`, `git`, `jq` (obrigatório — check no bootstrap)
- Versão canônica: `2.1` (arquivo `VERSION` é o SSOT)
- NUNCA fazer:
  - Reescrever skills inteiras — apenas adicionar seção/regra específica
  - Criar novas features ou comandos
  - Alterar interface pública das skills (triggers, nomes de seções obrigatórias)
  - Usar `sed -i` diretamente — usar `sed_inplace` de `lib/core.sh`

---

### ARQUIVOS PERMITIDOS
- `.devorq/skills/quality-gate/SKILL.md`
- `.devorq/skills/quality-gate/CHANGELOG.md`
- `.devorq/skills/pre-flight/SKILL.md`
- `.devorq/skills/pre-flight/CHANGELOG.md`
- `.devorq/state/lessons-pending/2026-04-07-bash-dual-use-source-guard.md` (atualizar status)

### ARQUIVOS PROIBIDOS
- `bin/devorq` — não tocar
- `lib/*.sh` — não tocar
- `tests/` — não tocar
- Qualquer arquivo fora de `.devorq/skills/`

---

### DONE CRITERIA
- [ ] `quality-gate/SKILL.md` contém regra do guard de source na seção de checklist
- [ ] `pre-flight/SKILL.md` contém verificação automática de `lib/**/*.sh`
- [ ] `quality-gate/CHANGELOG.md` registra a mudança com versão MINOR bumped
- [ ] `pre-flight/CHANGELOG.md` registra a mudança com versão MINOR bumped
- [ ] Lição marcada como `applied` no arquivo `.devorq/state/lessons-pending/`
- [ ] `./bin/devorq skill version quality-gate minor` executado
- [ ] `./bin/devorq skill version pre-flight minor` executado

---

### DECISÕES JÁ TOMADAS
- Guard escolhido: `[[ "${BASH_SOURCE[0]}" != "$0" ]] && return 0` (padrão Bash oficial)
- Classificação da lição: Cross-project concern / Reusable knowledge — não é pontual
- Pre-flight = aviso (não bloqueio) / Quality-gate = bloqueio obrigatório
- Scope desta task: APENAS as duas skills — sem spec formal (é incorporação de lição)

---

### ANTI-PATTERNS
- Não usar `if [[ ... ]]` alternativo — o guard canônico é o aprovado
- Não adicionar a regra apenas em um lugar — ambas as camadas são obrigatórias
- Não refatorar o restante das skills ao editar — escopo mínimo
- Não gerar nova spec para esta task — é `lessons apply`, não feature nova
- Não esquecer de versionar as skills com `./bin/devorq skill version <skill> minor`

---

### LIÇÃO A APLICAR
Arquivo: `.devorq/state/lessons-pending/2026-04-07-bash-dual-use-source-guard.md`
Pipeline: Gate 5 ✅ aprovado → Gate 6 (lessons validate) → Gate 7 (lessons apply) — execute na ordem
